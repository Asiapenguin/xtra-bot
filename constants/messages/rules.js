const Discord = require('discord.js');

export const embedRulesMessage = new Discord.MessageEmbed()
  .setColor('#0099ff')
  .setTitle('some title')
  .setURL('https://discord.js.org/')
  .setDescription('some description');